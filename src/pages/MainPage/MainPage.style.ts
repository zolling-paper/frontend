import {css} from '@emotion/react';

export const mainLayout = css({
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'start',
  alignItems: 'center',
  width: '100vw',
  height: '850vh',
  overflowX: 'hidden',
});