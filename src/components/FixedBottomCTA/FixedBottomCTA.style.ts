import {css} from '@emotion/react';

export const fixedBottomCTAStyle = css`
  position: absolute;
  display: flex;
  justify-content: center;
  align-items: center;
  bottom: 0;
  left: 0;
  right: 0;
  z-index: 20;
`;
